[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    live: hits real external APIs (OpenWeather, Google Pollen, LLM). Requires RUN_LIVE_API_TESTS=1 and keys in .env
addopts = -m "not live"
